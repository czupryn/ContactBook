{% extends '::base.html.twig' %}

{% block body -%}
    <h1>form for {{ contact_id }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('address') }}">
            Back to the list
        </a>
    </li>
{#    <li>{{ form(delete_form) }}</li>#}
</ul>
{% endblock %}
